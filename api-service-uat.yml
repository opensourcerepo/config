server :
  port : 8765
spring:
  cloud:
    gateway:
      routes:
        - id: common-service
          uri: lb://COMMON-SERVICE
          predicates:
            - Path=/common/**
          filters:
            - StripPrefix=1
        - id: USER-SERVICE
          uri: lb://HR-OPERATIONS-SERVICE
          predicates:
            - Path=/hrops/**
          filters:
            - StripPrefix=1
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    gateway:
      enabled: true
