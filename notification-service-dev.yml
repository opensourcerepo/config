server :
  port : 8085
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/notification_service
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: aditya.antisimilarity@gmail.com
    password: aely edsg mklk wnkl
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  firebase-configuration-file: learn-push-notification-fdcec-firebase-adminsdk-fbsvc-b8b2145dd1.json

